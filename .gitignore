# Environment - NEVER commit this
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
# Root-scoped: the only Python build artefact lives at the repo root. An
# unanchored `build/` also swallowed assets/ogx/*/build, the video build sheets.
/build/
.venv/
venv/
env/

# Generated content (optional - remove if you want to track outputs in git)
# outputs/

# Auto-trader runtime logs (regenerated on every run)
outputs/reports/autotrade_log_*.md

# IDE
.vscode/
.idea/
*.swp
