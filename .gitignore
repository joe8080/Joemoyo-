# Environment - NEVER commit this
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Generated content (optional - remove if you want to track outputs in git)
# outputs/

# Auto-trader runtime logs (regenerated on every run)
outputs/reports/autotrade_log_*.md

# OGX pipeline artefacts are TRACKED, unlike the auto-trader logs above.
# They are not cheaply regenerated: a full run is four agent passes and, once
# narration is rendered, real credit spend. They are also the only record of
# what the evidence gate accepted for a given episode. Do not add them here.

# IDE
.vscode/
.idea/
*.swp
