node_modules/

# Plates are fetched on demand — see fetch-images.sh / assets/images/manifest.json.
# Placeholder stand-ins are regenerated by make-placeholders.sh.
assets/images/*.png

# Render outputs, scratch frame captures, and verification frames
*.mp4
work-*/
.snapshots/
snapshots/
